[project]
name = "ai_agent_development"
version = "0.1.0"
description = "ai-agent-development using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=0.130.0,<1.0.0"
]

[project.scripts]
ai_agent_development = "ai_agent_development.main:run"
run_crew = "ai_agent_development.main:run"
train = "ai_agent_development.main:train"
replay = "ai_agent_development.main:replay"
test = "ai_agent_development.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
